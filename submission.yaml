# Coldline - Task 2.6
# File: submission.yaml
# Component: Student answer sheet
# Purpose: Record the revision identifier Alembic generated for your migration.
# Interacts With: Submission schema, public verifier, schema migration checks
# Sprint/Task: Sprint 2 - Project 2 / Task 2.6
# Concepts: Version-controlled schema evolution, deterministic assessment
# Tools: YAML

# Complete submission.yaml from the evidence sources named beside each answer group.
# Keep the supplied keys; replace empty strings using each field's stated type.
# Empty values and placeholder text fail verification.
# Use submission-sample.yaml for format only; do not copy its fictional values.
# Do not include secrets, personal data, or local absolute paths.

answers:
  # Task 2.6 — Step 1: Schema migration
  # Inspect data layer schema and author migration script.
  # Generate and author one reversible migration with Alembic. Its revision ID
  # identifies that schema change. Apply and verify it in Step 2, then verify
  # rollback in Step 3. Record the authored revision ID.
  # Source: docs/student/task-2-6-contract.md.
  # Read the ID from the generated file in migrations/versions/ (before the
  # first underscore), its `revision` line, or `poe migrate-current` after applying it.
  #
  # Enter the generated ID as a string: 4–64 letters, digits, or underscores.
  # The supplied baseline revision 0001baseline is not an accepted answer.
  migration_revision_id: ""
